<%= form_with(model: search_term, local: true) do |form| %>
  <% if search_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_term.errors.count, "error") %> prohibited this search_term from being saved:</h2>

      <ul>
      <% search_term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term %>
    <%= form.text_field :term %>
  </div>

  <div class="field">
    <%= form.label :timespan %>
    <%= select_tag "search_term[timespan]", options_for_select([ "hour", "day", "week", "month", "year", 'all' ]) %>
  </div>

  <div class="field">
    <%= form.label :last_check %>
    <%= form.datetime_select :last_check %>
  </div>

  <div class="field">
    <%= form.label :check_frequency %>
    <%= form.number_field :check_frequency %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
